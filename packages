#!/usr/bin/env bash

all(){
  cd pkgs
  ls -1 | xargs cat
}

lite(){
  cd pkgs
  ls -1 | grep -v ^optional | xargs cat
}

main(){
  case $1 in
  lite)
    lite
  ;;
  *)
    all
  ;;
  esac
}

main "$@" | grep -v '#' | grep . | sort -u
