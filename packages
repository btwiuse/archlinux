#!/usr/bin/env bash

all(){
  cd packages.d
  ls -1 | xargs cat
}

bare(){
  cat packages.d/bare
}

lite(){
  cd packages.d
  ls -1 | grep -v ^optional | xargs cat
}

main(){
  case $1 in
  bare)
    bare
  ;;
  lite)
    lite
  ;;
  *)
    all
  ;;
  esac
}

main "$@"
